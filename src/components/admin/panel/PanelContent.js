import React from "react"

export default function PanelContent(props) {
    const { active_panel } = props.panel_props

    return(
        <div id="Panel-content" className="d-flex flex-column m-auto w-100">

        </div>
    )
}